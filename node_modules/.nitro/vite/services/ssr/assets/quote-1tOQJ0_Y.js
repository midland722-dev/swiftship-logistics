import { useEffect, useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { CheckCircle2, Shield, Ship, Truck, Zap } from "lucide-react";
//#region src/lib/quote-api.ts
var API_BASE = "http://localhost/ships";
async function fetchQuote(params) {
	const qs = new URLSearchParams();
	Object.entries(params).forEach(([key, value]) => {
		qs.set(key, String(value));
	});
	const url = `${API_BASE}/php/process/quote_calc.php?${qs.toString()}`;
	const response = await fetch(url, {
		method: "GET",
		headers: { Accept: "application/json" }
	});
	if (!response.ok) throw new Error(`Quote request failed: ${response.status}`);
	return response.json();
}
//#endregion
//#region src/routes/quote.tsx?tsr-split=component
var speedConfig = {
	standard: {
		icon: Ship,
		label: "Standard",
		days: "5–8 business days"
	},
	express: {
		icon: Truck,
		label: "Express",
		days: "2–3 business days"
	},
	priority: {
		icon: Zap,
		label: "Priority",
		days: "Next business day"
	}
};
function QuotePage() {
	const [from, setFrom] = useState("Berlin, DE");
	const [to, setTo] = useState("Tokyo, JP");
	const [weight, setWeight] = useState(2.4);
	const [length, setLength] = useState(30);
	const [width, setWidth] = useState(20);
	const [height, setHeight] = useState(15);
	const [speed, setSpeed] = useState("express");
	const [insurance, setInsurance] = useState(true);
	const [quote, setQuote] = useState(null);
	const [loading, setLoading] = useState(false);
	const [error, setError] = useState(null);
	useEffect(() => {
		let cancelled = false;
		setLoading(true);
		setError(null);
		fetchQuote({
			from,
			to,
			weight,
			length,
			width,
			height,
			speed
		}).then((res) => {
			if (!cancelled) setQuote(res);
		}).catch((err) => {
			if (!cancelled) setError(err instanceof Error ? err.message : "Something went wrong.");
		}).finally(() => {
			if (!cancelled) setLoading(false);
		});
		return () => {
			cancelled = true;
		};
	}, [
		from,
		to,
		weight,
		length,
		width,
		height,
		speed
	]);
	const price = quote?.data?.price ?? "—";
	const breakdown = quote?.data?.breakdown;
	return /* @__PURE__ */ jsxs("section", {
		className: "container-x py-16 md:py-20",
		children: [
			/* @__PURE__ */ jsx("p", {
				className: "font-mono text-xs uppercase tracking-widest text-brand",
				children: "Shipping quote"
			}),
			/* @__PURE__ */ jsxs("h1", {
				className: "mt-2 font-display text-4xl font-bold md:text-5xl",
				children: ["Instant quotes. ", /* @__PURE__ */ jsx("span", {
					className: "text-brand",
					children: "No account needed."
				})]
			}),
			/* @__PURE__ */ jsx("p", {
				className: "mt-4 max-w-xl text-muted-foreground",
				children: "Enter your parcel details and we'll price every service tier in seconds."
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "mt-10 grid gap-6 lg:grid-cols-[1.4fr_1fr]",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "rounded-2xl border border-border bg-surface/60 p-6 md:p-8",
					children: [
						/* @__PURE__ */ jsxs("div", {
							className: "grid gap-4 md:grid-cols-2",
							children: [/* @__PURE__ */ jsx(Field, {
								label: "From",
								value: from,
								onChange: setFrom,
								placeholder: "City, Country"
							}), /* @__PURE__ */ jsx(Field, {
								label: "To",
								value: to,
								onChange: setTo,
								placeholder: "City, Country"
							})]
						}),
						/* @__PURE__ */ jsx("h3", {
							className: "mt-8 text-sm font-semibold uppercase tracking-wider text-muted-foreground",
							children: "Parcel"
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "mt-3 grid gap-4 md:grid-cols-4",
							children: [
								/* @__PURE__ */ jsx(NumField, {
									label: "Weight (kg)",
									value: weight,
									onChange: setWeight,
									step: .1
								}),
								/* @__PURE__ */ jsx(NumField, {
									label: "Length (cm)",
									value: length,
									onChange: setLength
								}),
								/* @__PURE__ */ jsx(NumField, {
									label: "Width (cm)",
									value: width,
									onChange: setWidth
								}),
								/* @__PURE__ */ jsx(NumField, {
									label: "Height (cm)",
									value: height,
									onChange: setHeight
								})
							]
						}),
						/* @__PURE__ */ jsx("h3", {
							className: "mt-8 text-sm font-semibold uppercase tracking-wider text-muted-foreground",
							children: "Delivery speed"
						}),
						/* @__PURE__ */ jsx("div", {
							className: "mt-3 grid gap-3 md:grid-cols-3",
							children: Object.keys(speedConfig).map((s) => {
								const cfg = speedConfig[s];
								const Icon = cfg.icon;
								const selected = speed === s;
								return /* @__PURE__ */ jsxs("button", {
									onClick: () => setSpeed(s),
									className: `flex flex-col items-start rounded-xl border p-4 text-left transition ${selected ? "border-brand bg-brand/10" : "border-border bg-background/40 hover:bg-surface"}`,
									children: [
										/* @__PURE__ */ jsx(Icon, { className: `h-5 w-5 ${selected ? "text-brand" : "text-muted-foreground"}` }),
										/* @__PURE__ */ jsx("div", {
											className: "mt-3 font-semibold",
											children: cfg.label
										}),
										/* @__PURE__ */ jsx("div", {
											className: "text-xs text-muted-foreground",
											children: cfg.days
										})
									]
								}, s);
							})
						}),
						/* @__PURE__ */ jsxs("label", {
							className: "mt-6 flex items-center gap-3 rounded-xl border border-border bg-background/40 p-4",
							children: [/* @__PURE__ */ jsx("input", {
								type: "checkbox",
								checked: insurance,
								onChange: (e) => setInsurance(e.target.checked),
								className: "h-4 w-4 accent-[var(--brand)]"
							}), /* @__PURE__ */ jsxs("div", {
								className: "flex-1",
								children: [/* @__PURE__ */ jsxs("div", {
									className: "flex items-center gap-2 text-sm font-medium",
									children: [/* @__PURE__ */ jsx(Shield, { className: "h-4 w-4 text-brand" }), "Add insurance ($4.50)"]
								}), /* @__PURE__ */ jsx("div", {
									className: "text-xs text-muted-foreground",
									children: "Cover contents up to $2,000 in case of loss or damage."
								})]
							})]
						})
					]
				}), /* @__PURE__ */ jsx("aside", {
					className: "lg:sticky lg:top-24 lg:self-start",
					children: /* @__PURE__ */ jsxs("div", {
						className: "rounded-2xl border border-brand/40 bg-gradient-to-b from-brand/15 to-surface p-6 md:p-8",
						children: [
							/* @__PURE__ */ jsx("div", {
								className: "text-xs font-mono uppercase tracking-widest text-brand",
								children: "Estimated price"
							}),
							loading && /* @__PURE__ */ jsx("div", {
								className: "mt-2 text-sm text-muted-foreground",
								children: "Calculating…"
							}),
							error && /* @__PURE__ */ jsx("div", {
								className: "mt-2 text-sm text-red-700",
								children: error
							}),
							!loading && !error && /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsxs("div", {
								className: "mt-2 font-display text-5xl font-bold",
								children: ["$", price]
							}), /* @__PURE__ */ jsx("div", {
								className: "mt-1 text-sm text-muted-foreground",
								children: speedConfig[speed].days
							})] }),
							/* @__PURE__ */ jsx("ul", {
								className: "mt-6 space-y-2 text-sm",
								children: [
									`${from} → ${to}`,
									`${weight} kg parcel`,
									`${length}×${width}×${height} cm`,
									`${speedConfig[speed].label} shipping`,
									insurance ? "Insurance included" : "No insurance",
									breakdown ? `Billable: ${breakdown.billable_kg} kg` : null
								].filter(Boolean).map((line) => /* @__PURE__ */ jsxs("li", {
									className: "flex items-start gap-2 text-muted-foreground",
									children: [/* @__PURE__ */ jsx(CheckCircle2, { className: "mt-0.5 h-4 w-4 shrink-0 text-brand" }), String(line)]
								}, String(line)))
							}),
							/* @__PURE__ */ jsx("button", {
								className: "mt-6 w-full rounded-sm bg-accent py-3 text-sm font-bold uppercase tracking-wider text-accent-foreground hover:opacity-90",
								children: "Book this shipment"
							}),
							/* @__PURE__ */ jsx("button", {
								className: "mt-2 w-full rounded-sm border border-border py-3 text-sm font-semibold hover:bg-surface",
								children: "Save quote"
							})
						]
					})
				})]
			})
		]
	});
}
function Field({ label, value, onChange, placeholder }) {
	return /* @__PURE__ */ jsxs("label", {
		className: "block",
		children: [/* @__PURE__ */ jsx("span", {
			className: "text-xs font-medium uppercase tracking-wider text-muted-foreground",
			children: label
		}), /* @__PURE__ */ jsx("input", {
			value,
			onChange: (e) => onChange(e.target.value),
			placeholder,
			className: "mt-2 w-full rounded-lg border border-border bg-background px-3 py-2.5 text-sm outline-none focus:border-brand"
		})]
	});
}
function NumField({ label, value, onChange, step = 1 }) {
	return /* @__PURE__ */ jsxs("label", {
		className: "block",
		children: [/* @__PURE__ */ jsx("span", {
			className: "text-xs font-medium uppercase tracking-wider text-muted-foreground",
			children: label
		}), /* @__PURE__ */ jsx("input", {
			type: "number",
			value,
			step,
			min: 0,
			onChange: (e) => onChange(Number(e.target.value)),
			className: "mt-2 w-full rounded-lg border border-border bg-background px-3 py-2.5 text-sm outline-none focus:border-brand"
		})]
	});
}
//#endregion
export { QuotePage as component };
